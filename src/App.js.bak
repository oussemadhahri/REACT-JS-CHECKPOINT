import React from 'react';
import 'bootstrap/dist/css/bootstrap.min.css';
import { Container, Card, Row, Col, Button } from 'react-bootstrap';
import Name from './components/Name';
import Price from './components/Price';
import Description from './components/Description';
import Image from './components/Image';

// Put your first name here to see the personalized greeting + image
const firstName = 'Oussema';

function App() {
  return (
    <Container style={{ padding: '2rem', maxWidth: 900 }}>
      <Row className="justify-content-center">
        <Col xs={12} md={8} lg={6}>
          <Card className="mx-auto shadow" style={{ maxWidth: 540 }}>
            <Image />
            <Card.Body>
              <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
                <div>
                  <Name />
                  <Price />
                </div>
                <Button variant="primary">Acheter</Button>
              </div>
              <Description />
            </Card.Body>
          </Card>

          <div style={{ marginTop: '1rem', textAlign: 'center' }}>
            {firstName ? (
              <>
                <h4>Hello, {firstName} 👋</h4>
                <img
                  src="https://media.giphy.com/media/xT1XGzE3l4N4GZoWkE/giphy.gif"
                  alt="wave"
                  width="120"
                  style={{ marginTop: '0.5rem' }}
                />
              </>
            ) : (
              <h4>Hello, there!</h4>
            )}
          </div>
        </Col>
      </Row>
    </Container>
  );
}

export default App;
